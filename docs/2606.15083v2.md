# REGRID-QAOA: A Resource-Efficient Hybrid QAOA Framework for Physics-Constrained Power System Islanding

**Autores:** YUQI JIANG, YUQI ZHANG, ZHIDING LIANG, QIANG GUAN, YAN LI, GANESH KUMAR VENAYAGAMOORTHY

## Abstract
Quantum computing has rapidly emerged as a powerful paradigm for tackling computationally demanding problems. In particular, quantum optimization shows strong promise for hard combinatorial problems in power systems, where increasing distributed energy penetration heightens the need for intentional islanding to maintain grid reliability and resilience. However, power system islanding is an NP-hard combinatorial optimization problem that becomes computationally prohibitive for classical solvers as network size grows, motivating the use of quantum computing as a promising alternative pipeline. This study develops a resource-efficient hybrid QAOA islanding framework that brings physics-constrained power-system partitioning into the quantum optimization workflow. 

## 1 Introduction
Recent advances in quantum hardware and algorithms have intensified interest in applying near-term quantum computing to structured optimization and simulation problems. In the noisy intermediate-scale quantum (NISQ) setting, hybrid quantum-classical methods are especially attractive because they combine parameterized quantum circuits with classical outer-loop optimization while avoiding the requirement of full fault tolerance.

Among the engineering domains that can benefit from such hybrid quantum optimization, power systems are especially compelling because many power grid operation decisions are inherently large-scale combinatorial problems defined on network topologies. 

## 2 Problem Formulation
A power grid is modeled as a weighted graph $G=(V,E)$ where $V=\{1,...,N\}$ is the set of buses and $E\subseteq V\times V$ is the set of transmission lines. Each transmission line $e_{ij}\in E$ connecting bus $i$ to bus $j$ carries an associated weight $p_{ij}$, representing the active power flow along that line in the direction from $i$ to $j$. 

To distinguish bus functionalities, the bus set $V$ is partitioned into three mutually disjoint subsets:
$$V=V_{G}\cup V_{L}\cup V_{0}$$

where $V_{G}$, $V_{L}$, and $V_{0}$ denote the sets of generator, load, and transit buses, respectively.
For each bus $i\in V$, let $p_{i}^{G}\ge0$ and $p_{i}^{D}\ge0$ denote the total active power generation and demand, respectively. The net active power injection at bus $i$ is then defined as:
$$p_{i}:=p_{i}^{G}-p_{i}^{D}$$

This work neglects network losses, so system-wide power balance requires the following conservation constraint:
$$\sum_{i \in V} p_i = 0$$

Accordingly, the three bus subsets are formally defined as:
$$V_{G}:=\{i\in V:p_{i}^{G}>0, p_{i}^{D}=0\}$$
$$V_{L}:=\{i\in V:p_{i}^{D}>0, p_{i}^{G}=0\}$$
$$V_{0}:=\{i\in V:p_{i}^{G}=0, p_{i}^{D}=0\}$$

When a disturbance occurs and islanding operation becomes necessary, the number of islands $K$ is pre-determined by the coherent groups of generators. Accordingly, the generator-bus set $V_{G}$ is partitioned into $K$ pairwise disjoint coherent groups:
$$V_{G}=\bigcup_{k=1}^{K}V_{G}^{(k)}$$

Each island is represented as a connected subgraph $G^{(k)}=(V^{(k)},E^{(k)})$ of $G$, where $V^{(k)}$ denotes the full set of buses assigned to island $k$. The island bus sets form a complete partition of $V$:
$$V=\bigcup_{k=1}^{K}V^{(k)}, \quad V^{(k)}\cap V^{(l)}=\emptyset, \quad \forall k\ne l$$

The set of transmission lines severed to form the islands, referred to as the cut set, is defined as:
$$E_{c}:=\{e_{ij}\in E:i\in V^{(k)}, j\in V^{(l)}, k\ne l\}$$

To ensure each island operates self-sufficiently following separation, the net active power injection must be balanced within each island:
$$\sum_{i\in V^{(k)}}p_{i}=0, \quad \forall k=1,...,K$$

The controlled islanding problem is then formulated as minimizing the total disrupted active power flow across the severed lines, subject to the partitioning, connectivity, and coherency requirements:
$$\min_{(V_{k})_{k=1}^{K}}\sum_{k,h\in\{1,...,K\}, k\ne h}\sum_{i\in V_{k},j\in V_{h}}\frac{|p_{ij}|+|p_{ji}|}{2}$$
s.t. 
$$V_{k}\cap V_{h}=\emptyset, \quad \forall k\ne h$$
$$\bigcup_{k=1}^{K}V_{k}=V$$
$$|V_{k}|\ge N_{min},\forall k\in\{1,...,K\}$$
$$|V_{k}\cap V_{G}|\ge N_{G,min},\forall k\in\{1,...,K\}$$
$$|V_{k}\cap V_{L}|\ge N_{L,min},\forall k\in\{1,...,K\}$$
$$\forall g,g'\in V_{G}: (c(g)\ne c(g')\Rightarrow\forall k:\neg(\{g,g'\}\subseteq V_{k}))$$
$$\forall g,g'\in V_{G}: (c(g)=c(g')\Rightarrow\exists k:\{g,g'\}\subseteq V_{k})$$
$$C(G[V_{k}])=1 \quad \forall k\in\{1,...,K\}$$

*[Descripción de la imagen (Figura 1): Diagrama general del marco propuesto para la operación en isla de sistemas de energía controlados basado en QAOA. Consiste en cinco bloques de operación: (a) configuración del problema que convierte la red del mundo real en un grafo, (b) codificación QAOA mediante representación QUBO, (c) ejecución y medición del circuito QAOA, (d) post-procesamiento clásico (M. 1-5) para reparar y filtrar candidatos, y (e) extracción de la solución final.]*

## 3 QAOA Solutions for Power System Islanding
### 3.1 Graph Reduction via Generator Coherency
For each $k\in\mathcal{K}_{c}$, all buses in $V_{G}^{(k)}$ are replaced by a single super-bus $v_{k}^{*}$. The reduced bus set is:
$$V'=(V\setminus\bigcup_{k\in\mathcal{K}_{c}}V_{G}^{(k)})\cup\{v_{k}^{*}:k\in\mathcal{K}_{c}\}$$
For every bus $v \notin V_{G}^{(k)}$, the aggregated edge weight from $v_{k}^{*}$ to $v$ is:
$$w'(v_{k}^{*},v)=\sum_{u\in V_{G}^{(k)}}w(u,v)$$

### 3.2 QUBO Conversion of the Objective Function
For each bus $i$, a binary variable $y_{i,k}$ is defined, where $y_{i,k}=1$ denotes that bus $i$ belongs to island $k$.
$$w_{ij}:=\frac{|p_{ij}|+|p_{ji}|}{2}$$
$$H_{cut}=\sum_{e_{ij}\in E}w_{ij}(1-\sum_{k=1}^{K}y_{i,k}y_{j,k})$$

Constraints are encoded as:
$$H_{s}=\lambda_{M}\sum_{k=1}^{K}(\sum_{i\in V}y_{i,k}-N_{min}-s_{k}^{(M)})^{2}$$
$$H_{gen}=\lambda_{G}\sum_{k=1}^{K}(\sum_{g\in V_{G}}y_{g,k}-N_{G,min}-s_{k}^{(G)})^{2}$$
$$H_{load}=\lambda_{L}\sum_{k=1}^{K}(\sum_{l\in V_{L}}y_{l,k}-N_{L,min}-s_{k}^{(L)})^{2}$$

The overall problem Hamiltonian for QAOA is constructed as:
$$H_{prob}=H_{cut}+\lambda_{h}\tilde{H}_{h}+\lambda_{M}\tilde{H}_{s}+\lambda_{G}\tilde{H}_{gen}+\lambda_{L}\tilde{H}_{load}+\lambda_{coh}\tilde{H}_{coh}+\mu\tilde{H}_{c}$$

## 4 Post-Processing Framework for Feasible Islanding
Because the islanding constraints sharply restrict the feasible region, many low-energy samples remain infeasible. Post-processing is therefore used to repair promising samples and improve the final solution quality and feasibility.

### Mathematical Preliminaries
The QUBO energy is:
$$Q(z)=H_{cut}(z)+\lambda_{h}\tilde{H}_{h}(z)+\lambda_{M}\tilde{H}_{s}(z)+\lambda_{G}\tilde{H}_{gen}(z)+\lambda_{L}\tilde{H}_{load}(z)+\lambda_{coh}\tilde{H}_{coh}(z)$$

The connectivity-violation objective is:
$$C(z):=\sum_{k=1}^{K}(c(G[V_{k}(z)])-1)$$

## 5 Case Study
The numerical study is conducted on six standard IEEE benchmark systems: 9-bus, 14-bus, 24-bus, 30-bus, 39-bus, and 57-bus.

*[Descripción de la imagen (Figura 2): Visualización de los resultados óptimos de segmentación en islas generados por QAOA en varios sistemas de prueba IEEE (9, 14, 24, 30, 39 y 57 nodos). Los colores de los nodos indican su asignación a una isla, y las líneas punteadas rojas representan las interfaces de transmisión cortadas.]*

*[Descripción de la imagen (Figura 3): Gráficos de barras que comparan la calidad promedio del corte (cut weight) y el tiempo de ejecución cuántico promedio (quantum runtime) a través de todos los sistemas de prueba IEEE para QAOA puro (Vanilla) y los métodos de post-procesamiento M.1 a M.5.]*

*[Descripción de la imagen (Figura 4): Mapa de calor (Heatmap) que muestra el porcentaje de desviación del valor de corte promedio con respecto al benchmark clásico (Gurobi) para todos los métodos a lo largo de los sistemas de prueba. Valores menores indican una mayor concordancia con el óptimo clásico.]*

*[Descripción de la imagen (Figura 5): Mapa de calor (Heatmap) que ilustra la probabilidad promedio de encontrar una solución factible a través de los diferentes sistemas de prueba para el modelo Vanilla y los métodos de post-procesamiento M.1 a M.5.]*

*[Descripción de la imagen (Figura 6): Gráficos de barras agrupadas comparando a nivel de isla la capacidad de carga y generación para las soluciones obtenidas. Para cada caso, se compara la demanda de potencia activa con la capacidad máxima de generación activa, y la demanda de potencia reactiva con la capacidad máxima reactiva.]*

## 6 Conclusion
This paper presents a hybrid quantum-classical framework for controlled power-system islanding and pioneers the application of QAOA for power-system islanding with explicit feasibility-critical constraints. The proposed framework combines quantum optimization with a structured post-processing design that converts sampled QAOA outputs into feasible islanding decisions when important physical requirements, such as exact connectivity and operational consistency, cannot be enforced by the QUBO model alone.
